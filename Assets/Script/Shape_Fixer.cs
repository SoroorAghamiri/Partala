/* program coded by AmirHossin Alishahi
in 1/30/2019
version : 1.0
*/


using UnityEngine;
using System.Linq;
using System.Collections.Generic;
public class Shape_Fixer : MonoBehaviour
{
  
            //for (int i = 0; i < gameObjects.Count; i++)
            //{
            //    gameObjects[i].transform.position =
            //        Vector3.Lerp(gameObjects[i].transform.position, Fix_Pose[i], speed * Time.deltaTime);
            //    gameObjects[i].transform.rotation = Quaternion.Lerp(gameObjects[i].transform.rotation, Fix_Q[i],speed * Time.deltaTime);
            //}
    
}
